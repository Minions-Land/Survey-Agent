<div align="center">

# 📚 Survey Agent 网页版使用指南

**Web Interface Guide**

[← 返回主文档](README.md) · [← Back to README](README_EN.md)

</div>

---

本文档详细介绍 Survey Agent 网页版的所有功能和操作方法。网页版与终端版功能完全一致，并额外提供可视化交互界面，适合不熟悉命令行的用户。

> This guide covers the web interface in detail. For CLI usage, see [README.md](README.md) / [README_EN.md](README_EN.md).

---

## 目录

1. [启动服务](#1-启动服务)
2. [界面总览](#2-界面总览)
3. [左侧配置面板](#3-左侧配置面板)
   - [3.1 参考资料](#31-参考资料)
   - [3.2 起始文档](#32-起始文档)
   - [3.3 语言模型配置](#33-语言模型配置)
   - [3.4 文献搜索配置](#34-文献搜索配置)
   - [3.5 输出设置](#35-输出设置)
4. [开始调研](#4-开始调研)
5. [流水线进度可视化](#5-流水线进度可视化)
6. [对话交互](#6-对话交互)
7. [分类法可视化抽屉](#7-分类法可视化抽屉)
   - [7.1 查看分类树](#71-查看分类树)
   - [7.2 调整置信度阈值](#72-调整置信度阈值)
   - [7.3 编辑分类体系](#73-编辑分类体系)
   - [7.4 调整论文上限](#74-调整论文上限)
8. [引用文献下载工具](#8-引用文献下载工具)
9. [界面设置](#9-界面设置)
10. [查找输出文件](#10-查找输出文件)
11. [部署到云端](#11-部署到云端)
12. [常见问题](#12-常见问题)

---

## 1. 启动服务

### 本地运行

```bash
# 确保已安装 web 依赖
# uv sync --extra web

# 启动网页服务（默认端口 8080）
survey-agent --web

# 指定端口
survey-agent --web --web-port 9000
```

启动后在浏览器访问：`http://localhost:8080`

### 使用环境变量预配置

如果已在 `.env` 文件中配置好 API Key，启动时无需在网页中填写：

```bash
# .env 中设置
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxx
SEMANTIC_SCHOLAR_API_KEY=xxxxxxxxxx  # 可选
```

---

## 2. 界面总览

```
┌──────────────────────────────────────────────────────────────────┐
│  📚 Survey Agent          [中/EN]  [☀️]  [状态徽章]              │  ← 顶栏
├──────────────────┬───────────────────────────────────────────────┤
│                  │  ┌─────────────────────────────────────────┐  │
│   左侧面板        │  │  流水线阶段进度                          │  │
│   ─────────      │  └─────────────────────────────────────────┘  │
│   📖 参考资料     │                                               │
│   · 主题输入      │  ┌─────────────────────────────────────────┐  │
│   · 论文 ID       │  │                                         │  │
│   · 标题搜索      │  │   对话气泡区域                           │  │
│   · PDF 上传      │  │   (Agent 左 / 用户 右 / 系统居中)        │  │
│   · 起始文档      │  │                                         │  │
│                  │  └─────────────────────────────────────────┘  │
│   🤖 语言模型     │                                               │
│   🔍 文献搜索     │  ┌─────────────────────────────────────────┐  │  ← 右侧主区域
│   📄 输出设置     │  │  聊天输入栏（对话期间启用）               │  │
│                  │  └─────────────────────────────────────────┘  │
│   [🚀 开始调研]   ├───────────────────────────────────────────────┤
│   [⏹ 停止]       │  ┌───────────────────────────────────────────┐│
│                  │  │  📊 分类可视化 ↑↑↑ 上拉展开               ││  ← 底部抽屉
│   ─────────      │  └───────────────────────────────────────────┘│
│   📥 下载引用文献 │                                               │
└──────────────────┴───────────────────────────────────────────────┘
```

界面分为三个区域：
- **左侧面板**：所有配置项和工具
- **右侧主区域**：流水线进度 + 对话输出 + 输入栏
- **底部抽屉**：分类法可视化（上拉展开）

---

## 3. 左侧配置面板

### 3.1 参考资料

这是最重要的配置区域，控制调研的起点和方向。

#### 调研主题

在文本框中输入你希望调研的主题，例如：
- `视觉 Transformer 在医学图像分析中的应用`
- `Large Language Models for code generation: a survey`
- `基于强化学习的机器人操控综述`

**建议**：主题描述越具体，文献搜索越精准。避免过于宽泛的描述（如"深度学习"）。

#### 论文 ID（可选但强烈推荐）

提供一到几篇你认为最核心的论文作为种子，可以是以下任意格式：

| 格式 | 示例 |
|------|------|
| Semantic Scholar ID | `204e3073870fae3d05bcbc2f6a8e263d9b72e776` |
| arXiv ID | `1706.03762` |
| DOI | `10.48550/arXiv.2010.11929` |

多个 ID 用**英文逗号**分隔：`1706.03762, 2010.11929`

> 提供种子论文可以显著提升搜索质量。如果不提供，系统会基于主题自动搜索，但可能不够聚焦。

#### 标题搜索

不知道论文 ID？点击「🔍 标题搜索」：

1. 在弹出的搜索框中输入论文标题关键词
2. 点击「搜索」或按回车
3. 在结果列表中点击论文卡片即可加入种子集
4. 已选论文会高亮显示

#### PDF 上传

已有论文 PDF？点击「📎 上传论文 PDF」：
- 支持单次上传一篇
- 系统自动从 PDF 中识别论文信息（标题、作者、年份）
- 识别完成后自动加入种子集

---

### 3.2 起始文档

如果你已经对这个课题有初步的了解、笔记或规划文件，上传起始文档可以让 Agent 基于你的已有工作展开对话，而不是从零开始提问。

**支持格式**：PDF / Markdown (.md) / Word (.doc .docx) / 纯文本 (.txt)

**使用方法**：
1. 点击「📝 上传起始文档」
2. 选择文件后等待「✓ 起始文档已就绪」提示
3. Agent 在对话阶段会先阅读文档内容，再提出针对性问题

**起始文档可以包含**：
- 已阅读过的论文列表
- 对该领域的初步理解
- 希望调研的具体问题
- 期望的综述结构规划

---

### 3.3 语言模型配置

#### Provider 选择

从下拉框选择你的 LLM 服务商：

| 选项 | 适用场景 |
|------|----------|
| **Anthropic (Claude)** | 推荐，综合效果最好 |
| OpenAI (GPT) | GPT-4o 写作质量好 |
| Google Gemini | 有免费额度，适合轻量测试 |
| DeepSeek | 国内访问友好，成本低 |
| Kimi (Moonshot) | 超长上下文，适合长文档 |
| Qwen (通义千问) | 中文理解好 |
| GLM (智谱 AI) | 国产模型，合规需求 |

#### API Key

输入对应服务商的 API Key。如果已在 `.env` 文件中配置，**可以留空**，系统自动读取环境变量。

#### 轻量模型 / 高质量模型

- **轻量模型**：用于批量论文信息提取，调用次数多，应选择成本低的模型
- **高质量模型**：用于对话交流和综述写作，影响最终质量

留空时使用各 Provider 的推荐默认值：

| Provider | 默认轻量模型 | 默认高质量模型 |
|----------|-------------|----------------|
| Anthropic | claude-haiku-4-5 | claude-opus-4-6 |
| OpenAI | gpt-4o-mini | gpt-4o |
| Gemini | gemini-2.0-flash-lite | gemini-2.5-pro |
| DeepSeek | deepseek-chat | deepseek-reasoner |

---

### 3.4 文献搜索配置

#### Semantic Scholar API Key（可选）

无 Key 时可以使用，但受到较严格的速率限制。如需大量搜索，建议在 [semanticscholar.org/product/api](https://www.semanticscholar.org/product/api) 申请免费 Key。

#### 启用的搜索源

勾选要使用的文献数据库：

- ✅ **Semantic Scholar**（推荐保持勾选）：引用网络扩展的基础，提供最全的引用关系数据
- ✅ **arXiv**（推荐保持勾选）：覆盖最新预印本
- ☐ **OpenReview**：主要覆盖 NeurIPS、ICLR 等顶会论文，可按需开启

---

### 3.5 输出设置

#### 文档格式

- **LaTeX (.tex)**：生成标准学术格式，配合 `.bib` 文件可直接编译为 PDF，适合投稿
- **Markdown (.md)**：轻量格式，适合在 GitHub / Obsidian / Notion 中查看

#### 分类法敏感度

控制调研过程中人工介入的频率：

| 选项 | 行为 |
|------|------|
| **Balanced（推荐）** | 大类变更（新增/合并主类别）需要你确认，小调整自动完成 |
| **Strict（全手动）** | 每次分类变更都需要你审核，控制感最强 |
| **Liberal（全自动）** | 所有分类变更自动执行，全程无打断 |

#### 生成思维导图

勾选后，调研完成时额外生成 Mermaid 格式的思维导图（`.md` 文件），可在 GitHub、Obsidian 等平台直接渲染。

#### 本地 PDF 目录

如果你有一批本地 PDF 文献，填入目录路径（如 `data/papers`），系统会扫描并纳入调研。点击「📁 选择」可通过文件对话框选择目录。

---

## 4. 开始调研

配置完成后，点击「🚀 开始调研」按钮。

### 开始前检查清单

- [ ] 填写了调研主题
- [ ] 选择了正确的 LLM Provider 并填写了 API Key（或在 `.env` 中已配置）
- [ ] 勾选了至少一个文献搜索源

### 停止调研

调研过程中可随时点击「⏹ 停止」按钮中断流程。已完成的阶段数据不会丢失（当前会话内有效）。

---

## 5. 流水线进度可视化

调研启动后，顶部出现流水线进度条，显示当前所处阶段：

```
● ─── ● ─── ● ─── ● ─── ● ─── ● ─── ●
对话   搜索   研究   提取   分类   写作   完成
```

- **实心深色**：已完成的阶段
- **高亮动画**：当前正在执行的阶段
- **空心灰色**：尚未开始的阶段

阶段说明：

| 阶段 | 内容 |
|------|------|
| **对话** | 与你对话，了解研究背景和先验知识（5–8 轮） |
| **搜索** | 检索已有综述，评估重复度 |
| **研究** | 双向扩展论文网络，构建文献知识图谱 |
| **提取** | 对每篇论文结构化提取贡献、方法、局限 |
| **分类** | 构建分类体系，分组论文 |
| **写作** | 逐节撰写综述，验证引用合法性 |
| **完成** | 生成输出文件 |

进度条下方有**子状态文字**，显示当前节点的具体操作（如"正在扩展第 12 篇论文的引用网络..."）。

---

## 6. 对话交互

Survey Agent 在多个阶段会暂停并等待你的回复，以对话气泡的形式呈现。

### 对话气泡说明

- **左侧气泡（蓝色/绿色）**：Agent 的问题或回复
- **右侧气泡（深色）**：你发送的消息
- **居中药丸形**：系统状态提示（阶段切换、完成通知等）

### 初始对话阶段

调研开始后，Agent 会先与你进行 4–8 轮对话，提取先验知识：

**典型问题示例**：
- "你对这个领域已有哪些了解？熟悉哪些核心工作？"
- "你最感兴趣的子方向或研究问题是什么？"
- "你希望这篇综述从哪个独特角度切入？"
- "有没有你认为一定要纳入的论文？"

直接在底部输入框打字，按回车或点击「发送」提交回复。

> 💡 **建议**：对话时尽量具体，提供你知道的论文名称、作者、方向等信息，这样 Agent 能更精准地展开调研。

### 快捷回复按钮

某些问题下方会出现预设的快捷回复按钮（如「跳过」「继续」「查看详情」），点击即可快速回应，无需手动输入。

### 其他交互点

**重复综述发现**：如果系统发现已有类似综述，会展示其信息并询问：
- 是否继续（带差异化角度）
- 还是终止当前任务

**分类法变更确认**（Balanced / Strict 模式）：当分类体系发生大类变动时，系统展示变更方案并等待你确认：
- 回复 `y`、`yes` 或 `确认` 接受变更
- 回复 `n`、`no` 或 `拒绝` 保持原方案
- 直接输入修改意见，Agent 会尝试调整

**大纲确认**：写作前展示章节大纲，可提出修改意见或直接确认开始写作。

---

## 7. 分类法可视化抽屉

底部的抽屉面板实时展示当前论文分类状态，在**研究和分类阶段**尤其有用。

### 7.1 查看分类树

**打开抽屉**：点击底部的「📊 分类可视化」标签栏，或向上拖动它。

抽屉展开后，左侧显示横向树状图：

```
[根主题]
    ├── [主类别 A]  ── [子类 A1] ── [论文列表]
    │                └── [子类 A2] ── [论文列表]
    ├── [主类别 B]  ── [子类 B1] ── [论文列表]
    └── [主类别 C]  ── ...
```

每个节点显示：
- 类别名称
- 包含的论文数量
- 鼠标悬停可预览论文列表

右侧「未分类池」显示置信度低于当前阈值的论文（橙色背景提示）。

### 7.2 调整置信度阈值

阈值滑块控制哪些论文被纳入分类（置信度 ≥ 阈值）、哪些被放入未分类池。

**操作方法**：
1. 拖动滑块（范围 0.0 ~ 1.0）
2. 橙色区域（两端）为"危险区"——阈值过低会纳入低质量论文，过高会排除很多有效论文
3. 拖动到目标值后，出现「确认」按钮
4. 点击「确认」应用新阈值，未达阈值的论文移入未分类池

> 推荐阈值范围：0.40 ~ 0.65（Balanced 敏感度下）

### 7.3 编辑分类体系

如果你对 Agent 自动生成的分类不满意，可以手动编辑：

1. 点击「✏️ 编辑分类」进入编辑模式
2. 编辑模式下可：
   - **拖动论文**：将论文从一个类别拖到另一个类别
   - **拖动类别**：重新组织类别的父子关系
   - **重命名**：点击类别名称即可编辑
   - **未分类池的论文**：可拖入任意类别完成手动分配
3. 编辑完成后：
   - 点击「✅ 应用修改」保存并让 Agent 基于新分类继续
   - 点击「✕ 取消」放弃所有编辑

### 7.4 调整论文上限

右上角「上限」输入框控制调研中纳入的最大论文数量：

- 默认值：200 篇
- 可以随时**调高**（如从 200 改为 300），系统会继续扩展
- 不能低于当前已分类的论文数量
- 调整后立即生效，无需重启

> 论文数量越多，调研越全面，但耗时和 API 费用也越高。建议先用默认 200 测试，满意后再增加。

---

## 8. 引用文献下载工具

这是一个**独立工具**，与主调研流程无关，可随时使用。位于左侧面板底部「📥 下载引用文献」区域。

**功能**：上传任意一篇论文的 PDF，系统自动：
1. 从 PDF 中提取参考文献列表
2. 逐条在 arXiv 和 Unpaywall 中查找全文
3. 列出可下载的论文并提供链接

**使用场景**：
- 读到一篇精彩综述，想批量下载它引用的所有论文
- 快速建立某个方向的本地文献库

**操作步骤**：
1. 点击「📄 选择 PDF」
2. 上传目标论文的 PDF
3. 等待系统提取（通常 10–30 秒）
4. 在结果列表中查看可下载的论文

---

## 9. 界面设置

顶栏右侧有两个快捷按钮：

### 语言切换（中 / EN）

点击切换界面显示语言：
- **中文**：所有 UI 文字显示为中文
- **English**：所有 UI 文字切换为英文

> 注意：切换语言仅影响界面文字，不影响 Agent 的回复语言（Agent 回复语言由对话内容决定）。

### 主题切换（☀️ / 🌙）

点击切换日间/夜间主题，设置在刷新后保留。

---

## 10. 查找输出文件

调研完成后会弹出提示框，告知文件保存位置。所有输出文件保存在项目的 `data/surveys/` 目录：

```
data/surveys/
└── survey_视觉Transformer_20260101_120000/
    ├── survey.tex          # LaTeX 综述正文
    ├── references.bib      # BibTeX 参考文献库
    ├── survey.md           # Markdown 版本（如选择 MD 格式）
    └── mindmap.md          # Mermaid 思维导图（如勾选）
```

### 编译 LaTeX 为 PDF

```bash
cd data/surveys/survey_视觉Transformer_*/
pdflatex survey.tex
bibtex survey
pdflatex survey.tex
pdflatex survey.tex
```

或使用 Overleaf：将 `survey.tex` 和 `references.bib` 一起上传到 [overleaf.com](https://overleaf.com)，选择编译引擎为 `pdfLaTeX + BibTeX`。

---

## 11. 部署到云端

### Render.com（推荐，免费套餐可用）

1. 将项目 Fork 并推送到你的 GitHub
2. 在 [render.com](https://render.com) 注册并新建 **Web Service**
3. 关联你的 GitHub 仓库，Render 自动识别 `render.yaml`
4. 在 Dashboard → Environment 中添加环境变量：
   ```
   ANTHROPIC_API_KEY = sk-ant-xxxxxx
   ```
5. 部署完成后获得公网 URL，可分享给团队使用

### Docker（自托管）

```bash
# 构建镜像
docker build -t survey-agent .

# 运行容器
docker run -d \
  -p 8080:8080 \
  -e ANTHROPIC_API_KEY=sk-ant-xxxxxx \
  -e SEMANTIC_SCHOLAR_API_KEY=xxxxxx \
  -v $(pwd)/data:/app/data \
  --name survey-agent \
  survey-agent
```

`-v $(pwd)/data:/app/data` 将输出文件挂载到本机，容器重启后不丢失。

### 反向代理（Nginx）

如需在 80/443 端口提供服务：

```nginx
server {
    listen 443 ssl;
    server_name survey.yourdomain.com;

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";  # 必须：支持 WebSocket
        proxy_set_header Host $host;
        proxy_read_timeout 3600;  # 长连接超时设为 1 小时
    }
}
```

> **重要**：WebSocket 连接需要 `Upgrade` 和 `Connection` 头，反向代理必须正确转发，否则实时输出功能失效。

---

## 12. 常见问题

### Q: 点击「开始调研」后没有反应？

检查：
- 浏览器控制台（F12）是否有 WebSocket 连接错误
- 服务端终端是否有报错信息
- API Key 是否填写正确（或已在 `.env` 中配置）

### Q: 调研进行到一半断开了，能续接吗？

当前版本不支持断点续传（计划中的功能）。建议：
- 适当降低「上限」论文数量（如 50–100 篇）减少单次运行时间
- 或使用 `--web-port` 配合 `tmux` / `screen` 保持服务端常驻

### Q: Agent 的对话问题是英文的，但我想要中文？

在调研主题中用中文描述，并在第一轮对话时用中文回复。Agent 会自动识别语言并切换。也可以在对话中直接告诉 Agent："请用中文交流"。

### Q: 分类树一直是空的？

分类树在**研究阶段**之后才开始填充（需要一定数量的论文被分类）。如果研究阶段结束后仍为空，可能是：
- 没有找到足够的论文（检查主题和种子论文）
- 分类 Agent 遇到错误（查看服务端日志）

### Q: LaTeX 输出中有 `\cite{???}` 这样的占位符？

这是防幻觉机制检测到非法引用时的标记。通常是 Agent 在写作时引用了未在知识库中的论文。可以：
- 在综述完成后手动替换
- 或增加种子论文数量，确保关键文献被纳入知识库

### Q: 网页部署后，WebSocket 经常断开？

在反向代理（如 Nginx）中增加超时设置：
```nginx
proxy_read_timeout 3600;
proxy_send_timeout 3600;
keepalive_timeout 3600;
```

### Q: 论文数量达到上限后能继续扩展吗？

可以。在底部分类抽屉的「上限」输入框中输入更大的数值，系统会继续从当前状态扩展新论文。

---

<div align="center">

遇到问题？提交 [Issue](https://github.com/your-username/survey-agent/issues) 或查阅 [SETUP.md](SETUP.md)

</div>
